import { atom } from "recoil";

export const companyNumberState = atom({
  key: "companyNumberState",
  default: null,
});

export const tableNumberState = atom({
  key: "tableNumberState",
  default: null,
});
